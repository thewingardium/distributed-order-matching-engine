# H2 Configuration (Default for Local Dev)
spring.datasource.url=jdbc:h2:mem:domedb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# Initial schema for Trades (H2 specific or generic SQL)
# spring.sql.init.mode=always

# TimescaleDB / PostgreSQL Configuration (Uncomment to use)
# spring.datasource.url=jdbc:postgresql://localhost:5432/dome_db
# spring.datasource.username=postgres
# spring.datasource.password=password
# spring.datasource.driver-class-name=org.postgresql.Driver
